using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Señuelo_Card : MonoBehaviour
{
     public string Name;
    public MANOS player;
    public bool invoked;
    public bool destroyed;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
